/* Requires the Docker Pipeline plugin */
pipeline {
    agent { docker { image 'amazon/aws-cli' } }
    stages {
        stage('build') {
            steps {
             
                    sh '/usr/bin/aws iam list-roles'
              
            }
        }
    }
}
