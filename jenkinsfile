/* Requires the Docker Pipeline plugin */
pipeline {
    agent { docker { image 'node:16.17.1-alpine' } }
    stages {
        stage('build') {
            steps {
                withAWS(region: 'us-east-2') {
                    aws iam list-roles
                }
            }
        }
    }
}
